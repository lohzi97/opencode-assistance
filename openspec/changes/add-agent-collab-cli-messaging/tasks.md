## 1. Body Input And Public Message Commands

- [ ] 1.1 Add shared text/file/stdin body readers, then unit-test direct text, file content, explicit stdin, and missing body errors.
- [ ] 1.2 Implement public-message set and clear commands, then test request payloads and human-readable/JSON output modes.

## 2. Messaging Commands

- [ ] 2.1 Implement send command, then test text, file, stdin, kind, hard flag, and server validation error propagation.
- [ ] 2.2 Implement ask and answer commands, then test parent/body/option request payloads and non-2xx handling.
- [ ] 2.3 Implement messages command filters, then test room-wide, session-scoped, member-scoped, since, limit, and JSON output behavior.
