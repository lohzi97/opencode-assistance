---
description: Remove a queued proactive task item
agent: sebastian
---

Arguments: `$ARGUMENTS`

Treat the argument as the queue ID and invoke `.opencode/scripts/remove-queued-task`.

Return the JSON output directly.
